{
  "name": "vemoni-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for Vemoni",
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
